import { styled } from "@linaria/react";
import { RowStack } from "../../Styles";

export const Container = styled(RowStack)`
  align-items: start;
  border-bottom: 0.1px solid var(--border-color);
`;
