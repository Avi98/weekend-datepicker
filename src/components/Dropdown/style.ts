import { styled } from "@linaria/react";
import { ColStack } from "../Styles";

export const DropdownWrapper = styled(ColStack)`
  padding: 0.1rem;
  border-radius: 2rem;
  border: 0.1rem solid var(--border-color);
  background-color: var(--bg-body);
`;
